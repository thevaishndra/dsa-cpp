#include<bits/stdc++.h>
using namespace std;

struct Node {
    int data;
    Node* next;

    Node(int val, Node* next1){
        data = val;
        Node* = next1;
    }
};

Node* traversalInLinkedList(vector<int> &arr){
    Node* temp = head;
    while(temp != nullptr) {
        cout<<temp-> data<<" -> ";
        temp = temp->next
    }
    return NULL;
}

